package cp125.week2;

/**
 * @author Stuart Maclean
 *
 * Trying to write a Java method to swap two ints.  Would be a static
 * method, since no objects are involved/needed.
 *
 * You might come to the conclusion that this cannot be done.  Why?
 */

public class SwapInts {

	static public void main( String[] unused ) {

		int i = 3;
		int j = 4;
		System.out.println( "i is " + i + ", j is " + j );
		swap( i, j );
		System.out.println( "After swap, i is " + i + ", j is " + j );
	}

	static void swap( int first, int second ) {
		// fill this in !!!
	}
}

// eof

		